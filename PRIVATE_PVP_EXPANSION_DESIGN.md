# Private PvP 拡張ルール — 実装前設計

最終更新: 2026-08-28
状態: **追加通常札・可変デッキ・終了条件・Blank、および6枚の比較型Tarotの純粋エンジン、Private Socket/UI接続まで実装済み。対象選択・秘匿・生成系のTarotは未接続**

この文書は、dub-227さんからの未確定仕様を待つ間に固められる設計を記録する。既存ルールの正本は常に `game-rules.js` と既存テストであり、この文書はそれを置き換えない。

カードごとの導入順、必要機能、混在禁止、追加・廃止の運用は
[`PRIVATE_PVP_CARD_CATALOG_DESIGN.md`](./PRIVATE_PVP_CARD_CATALOG_DESIGN.md) を参照する。

## 1. 守るべき境界

- 既定のPrivate PvP、ランダムマッチ、ソロ・ランク戦は、現在のクラシックルールのままとする。
- デッキ編集・追加カード・Blank・Tarotは、**Private PvPの拡張プリセットだけ**で利用できる。
- Ranked Solver、レート、シーズン、ランダムマッチのマッチング条件へ拡張設定を混入させない。
- 拡張ルールは一部の表示だけを切り替える機能ではない。開始時にサーバーが検証・固定したルールスナップショットを用いる独立した対局として扱う。
- クライアントはカードの能力、強さ、勝敗、得点、対象、乱数、終了結果を決めない。送れるのは、許可されたカード実体IDと、将来の対象選択で必要な一回限りの操作IDだけである。

## 2. ルールセットの二層構造

### クラシックPrivate

現在の `classic-v1`。7枚、90秒、7ラウンドまたは9枚先取、既存勝敗ロジックで固定する。制限時間だけは既存どおり60/90/120秒から選べる。

### 拡張Private

`private-expanded-v1` のような**バージョン付きプリセット**として導入する。クラシック7枚とTen〜Fourを同じ手札へ入れられ、待機中・終了後のルーム設定でデッキを編集できる。対局開始後には不変となる。

「自由にカードを入れたり消したりする」UIを直接ルール本体へつなげない。まずサーバーが受理できるデッキ構成を検証し、その正規化済み設定から同一の手札を両者に生成する。

## 3. デッキ編集の基本モデル

### 3.1 公平性

- 一つのPrivateルームには**一つの共通デッキ構成**を持たせる。
- 対戦者1・2には同じ定義・同じ枚数のデッキを配る。片方だけが任意のカードを多く持つ非対称デッキはv1では導入しない。
- 同名カードを複数入れる場合も、配布時にサーバーが別々の `instanceId` を発行する。選択・消費・履歴・獲得処理はカード名ではなく `instanceId` で行う。

### 3.2 編集データ

クライアントが扱うのは、能力を含まない宣言的な枚数だけとする。

```json
{
  "ruleset": "private-expanded-v1",
  "deck": [
    { "definitionId": "ace", "copies": 1 },
    { "definitionId": "joker", "copies": 1 }
  ],
  "turnTimeLimitMs": 90000,
  "roundLimit": 7,
  "scoreTarget": 9,
  "blankEnabled": false
}
```

`definitionId` と `copies` 以外のカード情報（強さ・能力テキスト・効果順・公開範囲など）は受け取らない。サーバー側のカードカタログを唯一の参照先とする。

### 3.3 提案する初期制限

以下は現在サーバーで強制している上限・下限である。

| 項目 | 提案 | 意図 |
| --- | ---: | --- |
| 1人あたりの初期札 | 5〜14枚 | 極端に短い・長い対局を避け、既存の安全上限内に保つ。 |
| 同一カード定義の枚数 | 標準は0〜3枚 | コピー効果がない通常札でも、同一札だけで構成されるデッキを抑制する。カードごとの例外はカタログに明示する。 |
| 1ルームの初期カード実体 | 最大28枚 | 両者14枚ずつ。現在のPrivate拡張基盤の上限64枚より十分低い。 |
| 最大手札 | 24枚 | 将来の生成札を考慮する絶対上限。通常のデッキ編集では超えない。 |
| 最大総ラウンド | 20 | 延長・生成系カードが追加されても無限化を防ぐ。 |
| 履歴 | 最大64件 | 表示・通信量・保存量を一定に保つ。 |

通常のデッキ編集には、少なくとも1枚の通常プレイ可能カードを要求する。Blankや将来の特殊札だけで構成したデッキを許すかは、そのカードの仕様確定後に判断する。

## 4. カードカタログと互換性判定

各カード定義には、UI表示用の名前・説明だけでなく、サーバー検証用のメタデータを持たせる。互換性を画面のdisabled状態だけに依存しない。

```js
{
  definitionId: 'example-card',
  category: 'classic' | 'normal-extra' | 'blank' | 'tarot',
  availability: ['private-expanded-v1'],
  maxCopiesPerDeck: 3,
  requiresFeatures: ['blank-semantics-v1'],
  excludesTags: ['incompatible-with-hidden-cards'],
  uniqueGroup: null,
  visibilityModel: 'public',
  implementationStatus: 'available'
}
```

### 判定規則

1. サーバーはすべての `definitionId` を既知のカードカタログで解決する。
2. そのカードが選択中のルールセットで利用可能かを確認する。
3. 枚数・初期札数・総カード実体・手札上限・ラウンド上限を検証する。
4. `requiresFeatures` が有効か、`excludesTags` と競合していないかを検証する。
5. `uniqueGroup` を持つカードは、同一グループから許可数を超えて選べないようにする。
6. 失敗時は設定全体を変更せず、利用者に「どのカードが、どの制限と競合したか」を日本語で返す。

これにより、たとえば伏せ札を前提とするカードと「全手札公開」前提のカード、Blank仕様が未導入のカード、同時に使えない大型効果を安全に混在禁止にできる。

## 5. 設定のライフサイクル

```text
待機中 / 終了後
  └─ 設定担当者が編集を提案
       └─ サーバー検証・正規化
            └─ room.rulesDraft を更新し、revision を +1
                 └─ 両対戦者の開始同意を必ず解除
                      └─ 両者が同じ revision へ開始同意
                           └─ 開始時に rulesSnapshot と配布札を固定
                                └─ 対局中は編集不可
```

- 設定担当者は待機中・終了後に限り、設定を変えずに接続中のもう一人の対戦者へ編集権限を譲れる。譲渡先をブラウザが指定することはできず、サーバーが同じPrivateルーム内の対戦相手だけを選ぶ。
- 権限の譲渡自体は設定内容もrevisionも変更しない。ただし旧担当者の編集要求は直ちに拒否し、新担当者だけへ画面内の引継ぎ通知を送る。
- 設定担当者が退出または観戦者へ切り替わった場合も、残った対戦者がいればサーバーが引継ぎを行い、新担当者へ通知する。
- 観戦者は設定を閲覧できるが、変更・開始同意・デッキ編集はできない。
- ランダムマッチとソロ・ランク戦からの設定変更イベントは、画面を隠すだけでなくサーバーで拒否する。
- 設定変更中の古いソケット要求には `roomId` と `revision` を照合する。古いrevisionへの開始同意、選択、対象指定は状態を変えず拒否する。

## 6. UI/UXの設計

### 6.1 ルーム待機画面

- 現在の「この部屋のルール」欄を要約表示の入口にする。通常のクラシックルームでは、今と同じ簡潔な表示を保つ。
- 拡張プリセットを選んだときだけ「デッキ編集」を展開できる。開始済み・観戦中では常に読み取り専用にする。
- デッキ一覧は、カード名、枚数の増減、合計枚数、互換性の状態、カード能力の短い説明を表示する。
- 無効な組合せは、単にボタンを灰色にするだけでなく、理由と解消方法を表示する。
- 設定変更で開始同意が解除されることを、変更前と変更後の両方で明示する。

### 6.2 モバイル

- カードカタログとデッキ一覧は一画面へ無理に詰め込まず、縦方向に展開する。
- 枚数操作は44px以上のタップ領域を確保し、長押しやドラッグ必須にしない。
- 合計枚数・開始可能状態・競合理由は設定欄の上部に表示する。
- 対局中の手札は既存どおり固定寸法の横スクロールとし、デッキ編集によってカード幅が伸縮しないようにする。

### 6.3 観戦者

- 観戦者には拡張ルールの名前・デッキ構成・公開情報を表示する。
- 使用中のTarotがある対局では、観戦者用のカード一覧を表示する。選択したカードのギリシャ記号・名称・能力だけを読み取り専用で示し、手札や対局状態を変更する操作には接続しない。
- 伏せ札・ノイズ等を導入したカードは、受信者別のviewを実装するまで有効化しない。
- 観戦者の空席参加予約・チャット・退室は既存の権限を維持する。デッキ編集やカード選択へ干渉させない。

## 7. サーバー状態と通信境界

開始前は編集用の `rulesDraft`、開始後は不変の `rulesSnapshot` を別に保存する。表示用のroom viewは、受信者の権限に応じて作る。

```text
room
├─ rulesDraft       # 待機/終了後にだけ更新可能
├─ rulesRevision    # 設定変更ごとに増加
├─ rulesSnapshot    # 開始時の不変コピー
├─ privateGameState # instanceId単位の手札・得点・履歴
└─ pendingAction    # 将来の対象選択。サーバーだけが保持
```

`pendingAction` は対象選択型カードが導入されるまで作らない。ただし導入時は、カード実体ID、許可対象、nonce、期限、対象者、元のrulesRevisionを結び付ける。観戦者・古い再接続・重複送信・改ざん対象をすべて状態変更なしで拒否する。

## 8. セキュリティ・堅牢性

- ルーム設定・デッキ更新イベントは、Privateの設定担当者、待機/終了状態、現在のrevisionに限る。
- ペイロードは小さく制限し、未知フィールド、負数、文字列の枚数、未知のカードIDを拒否する。
- サーバーは設定を正規化してから保存する。同一カードの重複エントリや順序差を統合し、結果をクライアントへ返す。
- カード能力を関数・式・HTMLとしてクライアントから受け取らない。カード説明も `textContent` で表示する。
- 生成・コピー・延長効果には、総カード実体、最大手札、最大ラウンド、1ラウンドの効果処理数、履歴件数という絶対上限を必ず適用する。
- 秘密情報を含むカードは、CSSで隠さない。相手・観戦者向け通信データから定義ID、強さ、能力を除いた専用viewを返す。
- 追加のルーム内イベントにも、既存の接続・IP・セッション単位のレート制限を適用する。
- 設定担当の譲渡イベントは、Privateルーム、現在の担当者、待機/終了状態、接続中の対戦者2人という全条件をサーバー側で検証する。観戦者・第三者・対局中の要求は状態を変えず拒否する。

## 9. 更新版提案で確定した点と、残る未確定事項

更新版の提案書により、Ten〜Fourの追加、60/90/120秒、総ラウンド終了時の獲得枚数比較、任意の即時勝利閾値、各Tarotの能力文が提示された。純粋エンジンとPrivate Socket/UIは、このうち追加通常札・凍結済み共通デッキ・総ラウンド終了・即時勝利閾値に加え、Death／Temperance／The Devil／The Tower／The Chariot／Strengthを実装済みである。強さはラウンド解決前の局面からサーバーで決定し、Jokerは決定後の強さをコピーする。Strengthは内部で強さを2倍単位の整数として計算する。The Chariotは相手の確定強さが15以上なら、通常の数値比較より先に勝つ。強さのスナップショットは履歴へ保存され、改ざんされた保存状態は拒否する。

「即時敗北条件」も候補として記載されているが、発動条件・優先順位が定義されていないため、現時点のルールスナップショットには含めない。

以下は勝敗・戦略・通信内容を変えるため、仕様または依存機能が揃うまで実装しない。

1. ロックは「ほかのカードがプレイされると解除」の“ほか”が、ロックされたプレイヤーだけを指すのか、両プレイヤーの任意のプレイを指すのか。
2. フリップ／ノイズの公開先と復元可能性。
3. Tarotの正確な処理順、コピー・生成の連鎖制限、対象選択仕様。

### Blankの確定仕様

- Blankはデッキへ入れない、手札外の再利用可能な仮想札である。
- 選択中のBlankは通常のカードと同様に確認できるが、確定しなければ何も送信されない。カードを未選択のまま確定する操作はできない。
- Blankを確定しても手札は減らず、Blank自体は獲得札・持ち越し札・得点にならない。相手がBlankへ勝った場合は、相手が実際に出した物理カードだけが得点対象になる。
- Blankが有効な部屋の時間切れでは、残った**合法な手札**とBlankを同じ候補集合としてサーバーが暗号学的乱数で選ぶ。無効な部屋では合法な手札だけから選ぶ。
- `mayPreventAllLegalPlays` を持つ将来カードがデッキに含まれるときだけ、Blankは必須にする。それ以外の拡張デッキでは、Blankの概念を無効にできる。

## 10. 回答後の実装順

1. 完了: 合意済みのカードカタログ、拡張プリセットのサーバー側検証、追加通常札、可変デッキ、総ラウンド終了・即時勝利の純粋エンジン、デッキ編集UI、設定revision・開始同意解除、Private Socket対局、Blank。
2. 再接続・観戦表示・スマホ幅を継続検証する。
3. 伏せ札・ロックを一機能ずつ仕様化して追加する。

各段階で、クラシックPrivate、ランダムマッチ、ソロ・ランク戦、観戦、チャット、再接続の回帰テストを実行する。未完成のカードはカタログに表示しても選択可能にしない。
