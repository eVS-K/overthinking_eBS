# Private PvP 拡張カードカタログ — 導入・互換性設計

最終更新: 2026-09-06
状態: **Ten〜Four、共通デッキ、Blank、Death／Temperance／The Devil／The Tower／The Chariot／Strength、The Magician／The Lovers／Wheel of FortuneはPrivate PvPで実装済み。対象選択・秘匿・履歴コピー系のTarotは未接続**

この文書は、dub-227さんの提案書にある追加通常カード、Blank、Tarot Cardを、安全に段階導入するための設計である。提案書に書かれたカード名・能力は候補であり、`available` と明記されるまで対局で使える仕様ではない。既存のクラシックルールの正本は `game-rules.js` と既存テストである。

関連資料:

- `PRIVATE_PVP_EXPANSION_DESIGN.md`: デッキ編集、設定凍結、通信境界、上限
- `CODEX_PRIVATE_EXPANSION_NOTES.md`: 作業メモと未確定事項
- `PRIVATE_PVP_REMAINING_TAROT_SPEC.md`: κ〜χ、Blank必須化、対象選択・秘匿・獲得札の確定仕様

> **実装前の正本:** 本文に残る「保留」「確認事項」は作成時点の経緯である。
> κ〜χの能力文、発動順、対象、競合、Blankの扱いについては、後から確定した
> `PRIVATE_PVP_REMAINING_TAROT_SPEC.md` を優先する。

## 1. カードを増やしても壊れない基本原則

### 1.1 カード定義は不変の識別子を持つ

カードは表示名ではなく、永続的な `definitionId` で識別する。日本語名・英語名・説明・UI上の分類は後から変更できるが、`definitionId` は変更しない。同じ定義を複数入れた札は、ゲーム開始時に別々の `instanceId` を与える。

公開済みのカード定義を物理的に削除してはならない。新規デッキから外したい場合は `retired` にして新しいルームで選べなくする。過去のルールスナップショット、履歴、再接続・観戦表示は維持する。

### 1.2 能力は許可済みの効果部品で表す

クライアントや設定ファイルから任意のスクリプトを実行する設計にはしない。サーバー側の純粋エンジンが持つ、監査済みの効果部品だけを組み合わせる。

最初に用意する効果部品の候補:

- 比較用強さの固定・条件分岐・倍率計算
- このラウンドに確定したカード情報のコピー
- カード実体の追加、破壊、ロック、ロック解除
- 対象カードの選択
- ラウンド上限の延長
- 獲得札・得点の移送または破棄
- 表示先ごとの公開、伏せ札、ノイズ

効果部品自体を追加・変更するときだけ、純粋エンジン、受信者別view、上限、テストをまとめてレビューする。通常のカード追加は、既存の効果部品と互換性メタデータを登録する作業に留める。

### 1.3 効果の解決順はバージョン固定する

カード定義には能力文だけでなく発動フェーズを持たせる。標準の候補順は次のとおり。

```text
常時効果
  → プレイ時効果
  → 強さ確定
  → 比較・勝敗
  → 勝利時 / 敗北時効果
  → 対象選択待ち
  → 後処理（獲得、スタック、解除、履歴）
```

この順番やカードの意味を変える場合は、カード単位でなく `private-expanded` の新しい評価バージョンとして扱う。既に始まった対局や履歴へ遡及適用しない。

## 2. カードカタログのメタデータ

各定義には、少なくとも次の属性を持たせる。

| 属性 | 用途 |
| --- | --- |
| `definitionId` / `contentVersion` | 不変の識別子と表示・仕様の版。 |
| 表示情報 | 名前、短い説明、カテゴリ、UI用の能力タグ。表示は常にテキストとして扱う。 |
| `availability` | 利用できるPrivate拡張プリセット。クラシック、ランダム、Rankedには含めない。 |
| `status` | `draft`、`specified`、`engine-ready`、`experimental`、`available`、`retired`。 |
| デッキ制限 | 1デッキあたりの上限、固有グループ、初期札数との相性。 |
| 必要機能 | `conditional-strength`、`lock-state`、`target-selection`、`card-generation`、`hidden-view` など。 |
| 競合タグ | 同時に使えない機能・カード群。サーバーが必ず判定する。 |
| 公開モデル | 常時公開、相手には伏せる、観戦者にも伏せる、ノイズなど。 |
| 上限消費 | 生成札数、対象選択数、効果ステップ、ラウンド延長の最大消費量。 |
| 再現情報 | 履歴に保存する最小の結果情報、乱数の有無、リプレイ時の必要情報。 |

能力を変えたい場合は既存定義を書き換えず、新しい `definitionId` または `contentVersion` を持つ後継カードを作る。これにより過去の対局を新しい能力で誤解釈しない。

## 3. 導入ロードマップ

### 3.1 常時有効: クラシック7枚

Ace、King、Queen、Jack、Joker、Three、Two。現在のPrivate PvP、ランダムマッチ、ソロ・ランク戦で使う唯一の既定デッキであり、拡張カードの導入によって挙動を変更しない。

### 3.2 第1段階: 追加通常札（実装済み）

| カード | 強さ | 能力 | 導入判定 | 必要機能・互換性 |
| --- | ---: | --- | --- | --- |
| Ten | 10 | なし | 最優先 | 通常の比較のみ。すべての公開カードと共存可能。 |
| Nine | 9 | なし | 最優先 | 同上。 |
| Eight | 8 | なし | 最優先 | 同上。 |
| Seven | 7 | なし | 最優先 | 同上。 |
| Six | 6 | なし | 最優先 | 同上。 |
| Five | 5 | なし | 最優先 | 同上。 |
| Four | 4 | なし | 最優先 | 同上。 |

この7枚は能力処理や秘匿情報を増やさず、現在の比較とカード実体管理だけで実装できる。拡張プリセットでは、これらとクラシックカードを待機中・終了後に任意に増減できる。

ただし、開始時のデッキ編集を有効にする前に、総ラウンド数・目標得点・カード不足時の終了条件を合意する必要がある。カードを増やせることと、終了条件を自動推測することは分けて扱う。

### 3.3 第2段階: 条件付き強さのTarot

| カード | 提案された能力 | 必要機能 | 先に確定すべき相性 |
| --- | --- | --- | --- |
| Death | 獲得枚数が相手以下なら13、そうでなければ0 | `conditional-strength` | 比較時点の獲得枚数、Joker等がコピーする対象。 |
| Temperance | 現在ラウンドが奇数なら14、偶数なら0 | `conditional-strength` | ラウンド番号が延長後も一意であること。 |
| The Devil | スタックがあれば15、なければ0 | `conditional-strength` | スタックの確認時点、Chariotとの優先順。 |
| The Tower | 現在ラウンド数 × 2 | `conditional-strength` | 整数の強さ表現、最大ラウンド上限。 |
| Strength | 自分の獲得枚数 × 1.5 | `conditional-strength` | 浮動小数点を使わず、強さを2倍単位の整数で比較する。 |
| The Chariot | 強さ15以上のカードに勝利する | `compare-override` | The Devil、Tower、Strength、コピー札との優先順。 |

Death、Temperance、The Devil、The Tower、Strength、The Chariotは実装済みで、Private拡張デッキへ各1枚まで入れられる。能力はラウンド解決直前の得点・持ち越し札・ラウンド番号から確定し、Jokerはその確定後の強さをコピーする。Strengthは強さを2倍単位の整数として計算し、表示・履歴には `4.5` のような表記だけを出す。The Chariotは相手の確定強さ（Jokerがコピーした値を含む）が15以上なら通常の数値比較より先に勝ち、両者が同時に満たす場合は引き分けにする。選択時はプレイヤー自身へ現在の強さと成立条件を表示し、解決後は公開済みの結果・履歴に強さを記録する。

### 3.4 第3段階: ロックと対象選択

| カード | 提案された能力 | 必要機能 | 主な競合・注意点 |
| --- | --- | --- | --- |
| Justice | 勝利時、相手のカード1枚をロック | `target-selection`、`lock-state` | 対象nonce、解除時点、時間切れ時の代替札。 |
| The Empress | 勝利時、相手の非Tarotを全てロック | `lock-state` | 対象上限、全札ロック時のBlank処理。 |
| The Sun | プレイ時、自分のカード1枚を破壊 | `target-selection`、`destroy-card` | 選択期限、手札不足、総カード数の整合性。 |

ロックは「ロックされた本人の次の解決済みラウンドの終了時に解除する一ターン制」と
確定した。全札ロック時はBlankを必須にする。正確な重複ロック・ノイズ対象の扱いは
`PRIVATE_PVP_REMAINING_TAROT_SPEC.md` を参照する。実装が未接続の間、ロックカードは
`specified` のままにする。

### 3.5 第4段階: 伏せ札・ノイズ

| カード | 提案された能力 | 必要機能 | 主な競合・注意点 |
| --- | --- | --- | --- |
| The Star | 相手が望む札1枚をノイズ状態で相手の手札へ追加 | `card-generation`、`hidden-view`、`target-selection` | 相手・観戦者それぞれに送る情報を分離し、正体をCSSだけで隠さない。 |

ノイズは所有者だけが定義を見られ、実プレイ時にだけ公開するものとして確定した。
受信者別room viewが完成するまで、観戦者が常に両手札を見られる現行クラシックの
仕様を拡張ゲームへ流用しない。詳細は `PRIVATE_PVP_REMAINING_TAROT_SPEC.md` を参照する。

### 3.6 第5段階: コピー・生成・ラウンド延長

| カード | 提案された能力 | 必要機能 | 主な競合・注意点 |
| --- | --- | --- | --- |
| The Fool | 自分の前ラウンドの強さ・能力をコピー | `round-snapshot`、`copy-effect` | 初回ラウンド、コピーがコピーを指す場合、再帰禁止。 |
| The Hermit | 相手の前ラウンドの強さ・能力をコピー | `round-snapshot`、`copy-effect` | 相手の情報公開、コピー範囲。 |
| The Magician | 相手が出した**実カード定義**のコピー2枚を自分へ追加 | `card-generation` | 実カードだけが対象。総実体数・手札上限に達すると追加数を縮める。 |
| The High Priestess | 敗北時、相手札1枚のコピーを自分へ追加 | `target-selection`、`card-generation` | 相手の伏せ札を対象にできるか、選択者と公開範囲。 |
| The Hierophant | 敗北時、自分札1枚のコピーを追加 | `target-selection`、`card-generation` | コピー元が伏せ札・ロック札の場合。 |
| The Lovers | 勝利時King、敗北時相手へQueenを追加 | `card-generation` | 生成枚数上限。引き分け時は不発。 |
| The Hanged Man | 敗北時、相手が望む札を相手手札へ追加 | `target-selection`、`card-generation` | 選択者の認可、対象定義の許可範囲。 |
| Wheel of Fortune | 勝利時は総ラウンド数を+1、敗北時は-1 | `round-extension` | 現在完了済みのラウンド未満には縮めず、最大20ラウンド。 |

The Magician、The Lovers、Wheel of Fortuneは実装済みで、比較・得点・持ち越しを確定した後に一回だけ解決する。The Magicianがコピーするのは相手の**カード定義**であり、コピーの能力をその場で再発動しない。追加札にはサーバー発行の新しい`instanceId`を与え、手札24枚・両者合計64実体・総ラウンド20回の上限を超えない。Wheel of Fortuneは凍結済みの対局内`effectiveRoundLimit`だけを変え、次戦用のルール設定は変えない。結果履歴には追加した枚数とラウンド変化を公開するが、内部の生成`instanceId`は送らない。

The Fool／The Hermitは、解決済み強さと安全な勝敗判定能力だけを反響し、反響の
再帰や対象選択・秘匿・獲得札操作はコピーしないものとして確定した。詳細は
`PRIVATE_PVP_REMAINING_TAROT_SPEC.md` を参照する。

### 3.7 高複雑度Tarot（仕様確定、実装は保留）

| カード | 保留理由 |
| --- | --- |
| The Emperor | 現ラウンドTarotだけを無効化し、Tarot相手には比較前に勝つ。比較器へ未接続。 |
| The Moon | `wonPile`から過去の獲得札を破棄する。物理札台帳へ未接続。 |
| Judgement | 過去に出した物理札を古い順に再生成する。履歴台帳へ未接続。 |
| The World | 相手のラウンド開始時の獲得札を破棄し、その定義コピーを加える。対象操作と物理札台帳へ未接続。 |

## 4. Blankの扱い（実装済み）

Blankは強さ0の**手札外・再利用可能な仮想札**として実装する。デッキ編集の枚数には含めない。

1. 拡張Privateで「Blankを使う」を有効にした場合、プレイヤーは通常の札と同じようにBlankを選択・確定できる。未選択のまま確定する操作はできない。
2. Blankは手札を消費せず、獲得札・持ち越し札・得点にはならない。物理カードとの勝負で勝者が得るのは、物理カードだけである。
3. 時間切れでは、合法な手札とBlankからサーバーが暗号学的乱数で一つを選ぶ。Blankが無効なら候補は合法な手札だけである。
4. ロック等で全手札が選べなくなり得るカードを将来有効化する場合、カードカタログの`mayPreventAllLegalPlays`によりBlankを必須にする。通常札だけのデッキでは、Blankを無効にしたまま遊べる。

Blankは通常のデッキカードではないため、Moonなど将来の獲得札移送・生成効果は、Blankを対象外として明示的に扱う。仕様を変更する場合は新しいBlank定義／ルールセット版として扱う。

## 5. 互換性をデータで管理する

### 5.1 機能タグによる許可判定

拡張プリセットは「有効な機能タグ」と「許可カード集合」を持つ。カードは必要機能が満たされ、競合タグがなければ選択できる。例:

| カード群 | 必須タグ | 混在禁止または保留条件 |
| --- | --- | --- |
| Ten〜Four | `public-cards` | なし。終了条件だけはプリセット全体で必要。 |
| Blank | `blank-semantics-v1` | 仮想・非消費・非得点。全札ロック可能カードでは必須。 |
| Death〜Tower | `conditional-strength-v1` | コピー・Chariotを含むなら比較優先順が必要。 |
| Strength | `scaled-strength-v1` | 浮動小数点比較は禁止。 |
| Chariot | `compare-override-v1` | 15以上の判定と特殊勝利の優先順が必要。 |
| Justice / Empress | `lock-state-v1`、`target-selection-v1` | ロック解除と全札ロック時のfallbackが必要。 |
| Star | `hidden-view-v1`、`card-generation-v1` | 受信者別viewが未導入なら禁止。 |
| Fool / Hermit | `round-snapshot-v1`、`copy-effect-v1` | コピー深度・初回ラウンドの仕様が必要。 |
| 生成系 | `card-generation-v1` | 総実体・手札・履歴・効果回数の上限が必須。 |
| Wheel | `round-extension-v1` | 最大総ラウンドと終了条件の再判定が必要。 |
| Emperor / Moon / Judgement | 専用タグ | 個別の仕様と統合テストが完成するまで禁止。 |

### 5.2 競合は双方向に検証する

カードAがカードBを拒否する場合、UIで片方だけを無効にするのでは不十分である。サーバーは最終デッキ集合を正規化した後、A→BとB→Aの両方を検査する。

エラーは「このカードは使えません」ではなく、たとえば「The Starには伏せ札表示機能が必要です。この部屋の拡張プリセットではまだ有効化されていません」のように返す。

### 5.3 バランスは互換性と分ける

カードが技術的に共存できることと、ゲームとして望ましいことは別である。カードカタログには、技術的な許可に加えて `experimental` と `balance-reviewed` を分けた状態を持たせる。

- 技術的に未完成: 選択不可。
- 技術的に安全だが検証中: Privateの明示的な実験プリセットでのみ選択可能。
- バランス確認済み: 通常の拡張プリセットで選択可能。

## 6. デッキ編集UIの将来像

カード一覧は「すべてを一度に有効化する場所」ではなく、カードカタログの状態を明確に見せる場所にする。

- 上部に、合計枚数、開始可能か、設定revision、両者の開始同意状態を表示する。
- 各カードには枚数の増減、最大枚数、必要機能、競合理由、能力説明を表示する。
- `draft` / `specified` のカードは、将来予定として見せる場合でも増減操作を許可しない。
- カードを増減すると、サーバー検証後の正規化済みデッキを表示し、両者の開始同意を解除する。
- 対局開始後と観戦者画面は、カード構成とルール版を読み取り専用で表示する。
- スマホではカード一覧を縦に並べ、枚数操作と状態説明を同じカード行に詰め込みすぎない。

## 7. カード追加・変更・廃止の運用手順

1. 提案: 名前、能力文、発動フェーズ、対象、公開範囲、上限、失敗時の扱いを一枚の仕様として確定する。
2. 分類: 必要効果部品・競合タグ・デッキ上限・テストケースを割り当てる。
3. 純粋エンジン: UI・Socket・DBなしの状態遷移テストを追加する。
4. 通信境界: 相手・観戦者・再接続者ごとのviewに漏洩がないことを検査する。
5. 実験導入: Privateの明示的な実験プリセットだけで有効化し、クラシック・Random・Rankedでは拒否する。
6. 公開: バランスと回帰確認後に `available` へ進める。
7. 廃止: 新規選択を止めるだけにし、履歴・進行中ゲーム・過去のリプレイ用定義は残す。

## 8. カードごとの必須テスト

各カードは少なくとも次を満たしてから `experimental` 以上へ進める。

- 発動・不発・境界値の純粋エンジンテスト。
- クラシック7枚と混在したときの勝敗・得点・スタック不変条件。
- 許可されないデッキ構成、枚数、対象、古いrevision、観戦者操作の拒否。
- 生成・コピー・延長の上限到達と、二重送信・再接続後の一回性。
- 伏せ札・ノイズを伴うなら、相手・観戦者・履歴APIへの情報漏洩検査。
- 同一ルールスナップショットから同じ結果を再現できること。

## 9. 現時点の決定と残る確認事項

更新版の提案書により、Ten〜Four、総ラウンド終了時の獲得枚数比較、任意の即時勝利閾値、および全Tarotの能力文が提示された。Ten〜Four、Blank、同一の凍結済みデッキを双方に配る純粋エンジン、総ラウンド・即時勝利の終了判定、6枚の比較型Tarot、対象不要の生成／総ラウンドTarot 3枚は実装済みである。ロック、伏せ札、履歴コピー、対象選択を伴う生成、残るTarotは上記の依存機能が仕様・テストともに揃うまで有効化しない。

κ〜χについて、追加の確認待ちで実装を止める必要はない。実装時は
`PRIVATE_PVP_REMAINING_TAROT_SPEC.md` の発動順、対象期限、不選択時の乱数、
受信者別公開をそのまま満たすことを要件にする。
