# OVERTHINKING UI / UX・演出改修 実装台帳

最終更新: 2026-09-09

## Experience North Star

**考える間は静かで読みやすく、伏せた瞬間に緊張が生まれ、公開時には「何が・誰に・なぜ起きたか」が一目で腑に落ちる対戦卓にする。**

対象は、入口、Private／Random／観戦／拡張Private／Ranked の表示体験、状態説明、カード選択、公開、能力効果、終局、再接続、モバイル、キーボード、`prefers-reduced-motion`、回帰契約である。

対象外は、ゲームルール・得点式・Ranked solver／rating・認証方式・DB schema・通信権限・公開／デプロイ・外部トラッキングの変更である。必要な表示用の安全な公開データは、既存のサーバー権威と受信者別 view を保った範囲でのみ追加できる。

## 現行照合メモ

### 実装済みとして確認したもの

- `main.js` には選択カードの能力説明、確定、公開、得点増加、勝敗、終局、対象カード選択、観戦、開始同意、Random再検索、再接続状態の表示がある。
- `private-room-view.js` は受信者ごとの投影を行い、他者には Noise の実体を渡さない。Noise は他者側では末尾に置かれ、札の並びで正体を推測しにくい。
- `private-game-engine.js` とサーバーが Private拡張のカード・対象選択・期限・終局を正本として扱う。
- 拡張Privateのデッキには α〜χ のTarotが存在し、設定編集・設定担当・開始同意・Blank・保存プリセットの表示がある。
- `ranked-client.js` は対局中の最適手／Q値／Regretを表示せず、終了後に判断成績・運・レートを表示する。
- CSSにはカード、公開、得点、効果、終局、スクロール誘導、モバイル、`prefers-reduced-motion` の既存資産がある。

### 実画面で確認したもの

- ローカルでPrivateの入室、開始同意、選択、確定、公開、引き分け、履歴、観戦、参加予約、Random検索／中止、拡張設定を確認した。
- 観戦チェック時も入口主ボタンが「入室する」と表示される摩擦を確認した。
- 320px級の拡張設定画面では文書全体の横はみ出しを確認しなかった。360×800、390×844、1440×900の厳密な視覚回帰は未実施である。
- Rankedはローカルの認証／DB未設定により、安全な利用不可表示までのみ確認した。認証済みRankedの実対局は未確認である。

### 資料の扱い

- `PRIVATE_PVP_EFFECT_UX_REMEDIATION_DESIGN.md` は現行コードと多くが整合するが、実装正本ではない。
- `CODEX_PRIVATE_EXPANSION_NOTES.md` 等にはTarot未実装時点の記述があり、現在のコードと不一致の過去記録として扱う。
- 本台帳、現行コード、テスト、安全なローカル実画面の順で事実を確定する。

## 絶対に壊してはいけない境界

- 勝敗、得点、タイマー、乱数、能力、設定、対象選択、終局はサーバー権威のままにする。
- クライアントは勝敗理由、能力対象、未公開選択を推測・生成しない。
- Noise・未公開札・受信者別投影の秘匿情報を、表示イベント、ARIA、DOM属性、アニメーション種別を通じても漏らさない。
- Private拡張をクラシックPrivate、Random、Rankedへ流入させない。Randomは観戦切替不可の既存契約を保つ。
- 観戦者、古いrevision／nonce、再送、再接続により操作権限や状態遷移を増やさない。
- キーボード、フォーカス復帰、44px級タップ領域、日本語可読性、長いカード名、reduced-motionを後付けにしない。

## フェーズ進捗

### Phase 0 — 表示イベント・公開境界・回帰契約

- [x] 表示イベントの scope、ID、一回性、優先度、終局優先を持つ共通モジュールを追加する。
- [x] 初期描画／再接続／再送で既存の公開結果を再演出・再読み上げしない。
- [x] PvPとRankedでreduced-motionの表示契約を共有する。
- [x] 表示イベントには安全な公開IDだけを使い、秘密の札や候補を持たせない。
- [x] 単体テストでdedupe、scope切替、優先度、初期hydrateを固定する。

### Phase 1 — 入口・観戦CTA・状態説明

- [x] 観戦選択時に入口の主CTAを「観戦する」へ変更する。
- [x] Private／Randomの目的と現在の操作を明快にする。
- [x] 待機、開始同意、設定編集中、再接続、観戦予約の状態説明を共通バナーへ整理する。
- [ ] モバイルでも主操作と残り時間を優先するための基礎レイアウトを整える（360px／390pxの実画面確認と合わせて継続）。

### Phase 2 — 対局ループと公開演出

- [ ] 選択、確定、相手待ち、公開、得点、持ち越し、効果、次操作を一つのラウンドストーリーとして表示する（既存資産を次の単位で整流する）。
- [x] 主操作を「カード未選択／確定可能／伏せ札済み／通信復帰中」で区別し、次に必要な操作だけを示す。
- [x] 終局を最優先にし、終局後の不要な効果／対象選択演出を抑止する。
- [x] サーバー投影の理由コードが必要なら、ゲーム判定を変えず安全な説明用データだけを追加する（クラシック比較理由）。

### Phase 3 — 拡張Privateの作業台

- [x] デッキを採用状況・カード種別で探索・比較できるようにする（役割別の追加絞り込みは実利用を見て継続判断）。
- [ ] 有効な概念だけを説明し、対象選択時に発動元→対象→結果を明確にする。
- [x] 対象操作の新規開始・自分の対象選択だけを一回通知し、候補札の選択状態をキーボードでも判別できるようにする。
- [x] 生成、ロック、ノイズ、破壊の効果フィードを統一する。

### Phase 4 — モード固有の磨き込み

- [x] Randomの検索、相手退出、再検索を安心して理解できる状態にする。
- [x] 観戦の左右対応、参加予約、退出を整理する。
- [x] Rankedの結果・判断成績・運・レートの理解を分離する。
- [ ] 認証済みRankedと再接続は構成可能な隔離環境で確認する。

### Phase 5 — 音・仕上げ・視覚回帰

- [ ] 音は任意設定のまま、視覚だけでも成立する効果音設計にする。
- [ ] 実ブラウザの視覚回帰、長文、縮小表示、reduced-motionを自動・手動で確認する。
- [ ] 外部トラッキングは導入せず、必要な仮説は将来提案として残す。

## チケットと受け入れ条件

### UX-001 表示イベント契約

- 対象: `presentation-events.js`、`main.js`、`ranked-client.js`、関連テスト。
- 受け入れ条件: 同じ公開イベントは同一scopeで一度だけ演出・読み上げされ、初期hydrateでは静的なラベル付き領域として表示される。終局は低優先度の効果演出と二重の結果読み上げを抑止する。
- 検証: unit test、Privateの公開／終局／再接続、安全なローカル画面。

### UX-002 入口と観戦CTA

- 対象: `index.html`、`main.js`、`style.css`、`pvp-ui.test.js`。
- 受け入れ条件: 観戦選択時にCTA、補足、ARIAが観戦を表し、Randomでは観戦選択肢が露出しない。
- 検証: mouse、touch、keyboard、空欄、Private／Random切替。

### UX-003 状態バナー

- 対象: `index.html`、`main.js`、`style.css`。
- 受け入れ条件: 待機、同意、設定編集、観戦予約、再接続、送信待ちを、主操作・次の行動と共に一箇所で理解できる。
- 検証: 開始同意、設定担当移譲、切断、観戦、Random検索。

### UX-004 Battle Stage／ラウンドストーリー

- 対象: `main.js`、`style.css`、必要時 `private-room-view.js`。
- 受け入れ条件: 公開後に勝敗、比較理由、得点／持ち越し、効果、次の行動が順序立って分かる。秘密を推測しない。
- 検証: Joker、2、3、引き分け、投了、複数Tarot、終局。

### UX-005 拡張デッキ作業台

- 対象: `main.js`、`style.css`、`private-ruleset.js` の表示メタデータのみ。
- 受け入れ条件: 現在採用中、役割、競合・Blank要件を見落とさず編集できる。
- 検証: 最小／最大デッキ、競合、保存プリセット、mobile。

### UX-006 モード固有画面

- 対象: `main.js`、`ranked-client.js`、`ranked.html`、各CSS。
- 受け入れ条件: Random、観戦、Rankedそれぞれの目的・制約・次操作を混同しない。
- 検証: 検索中止、相手退出、観戦予約、未認証／認証済みRanked。

### UX-007 視覚・アクセシビリティ回帰

- 対象: UIテスト、将来のブラウザ視覚テスト。
- 受け入れ条件: 360×800、390×844、1280×720、1440×900で意図しない文書横スクロールがなく、keyboardとreduced-motionで情報が欠落しない。

## 検証計画

- 画面: 360×800、390×844、1280×720、1440×900、必要なら横向き。
- 入力: mouse、touch、keyboardのみ。
- 状態: 待機、設定編集、開始同意、選択、確定待ち、公開、引き分け、能力対象選択、終局、再戦、切断、再接続。
- 役割: 対戦者、観戦者、参加予約中、Random、Private拡張、Ranked。
- 秘匿: Noise、未公開選択、古い状態更新、受信者別view。
- 動き: 通常、`prefers-reduced-motion`。
- 実行しないもの: 本番認証、DB破壊試験、デプロイ、コミット、外部送信、個人データ収集。

## 実施ログ

### 2026-09-08 — 開始前ベースライン

- 状態: 実施済み。
- 既存の未追跡ファイル `UI_UX_IMPLEMENTATION_EXECUTION_PROMPT.md` と `UI_UX_REDESIGN_PLANNING_PROMPT.md` を確認し、変更対象外として保持する。
- tracked fileの差分は開始時点でなし。
- `node scripts/check-syntax.js` は直前の調査で80 JavaScriptファイル成功。
- UI関連の `node --test pvp-ui.test.js ranked-ui.test.js ranked-client.test.js` は33件成功。

### 2026-09-08 — Phase 0／Phase 1

- 状態: 最初の実装単位を完了。
- 追加: `presentation-events.js` を追加。表示イベントは `id`、種別、優先度、排他性だけを保持し、任意payload・秘密の札・対象候補を保持しない。PvP／Rankedの初期hydrate・重複更新・再接続時は静的表示にし、終局は公開／効果より優先する。
- 追加: 入口の観戦CTA、モード説明、観戦時の補足、意味別の状態バナーを追加。Private／Random／観戦／開始同意／設定編集／再接続の現在地を一箇所で読めるようにした。
- 追加: クラシックの公開結果には、既存 `game-rules.js` の正本判定から投影した比較理由だけを表示する。クライアント側で勝敗や例外を推測しない。
- 追加: 拡張デッキの一覧は、ローカル表示だけを「すべて／採用中／Tarot／通常札」で絞り込み可能にした。設定送信、札の実体順、ルール、権限には影響しない。
- 検証: `node scripts/check-syntax.js` 成功。対象テスト48件成功。全体 `npm test` は254件成功。ローカル入口の実画面で、モード説明、観戦前の役割説明、主CTAの階層を確認した。
- 残るリスク: 認証済みRankedの実対局はローカル認証／DB未設定のため未確認。360×800、390×844、1280×720、1440×900の厳密な実ブラウザ回帰と、Private拡張のフィルター操作は次の検証単位で実施する。
- 次: モバイル幅の実画面を確認し、必要なレスポンシブ調整後に、対局中のラウンドストーリーと拡張効果フィードを一つずつ整備する。

### 2026-09-09 — Phase 2／Phase 3 効果の視覚言語

- 状態: 完了（拡張効果フィード）。
- 追加: `effect-language.js` を追加。公開済みの効果種別だけを、破壊・移動／ロック／ノイズ／追加・複製／ラウンド変化／発動なしへ分類する。カード実体、対象ID、nonce、未公開札などは保持・参照しない。
- 追加: 紫の効果帯を維持したうえで、短い記号とラベルを追加した。`✦ 追加・複製` は生成札の既存グリッチ表示と同じ意味を持ち、ロック・破壊・ノイズ等も主効果だけを短く演出する。複数効果は帯と履歴に全分類を表示し、舞台上の一瞬のバーストは優先度最高の一つに限定する。
- 実画面: 隔離ローカルの拡張Privateで θ The Lovers を追加し、Three に勝たせた。紫の帯、`✦ 追加・複製`、King追加時のグリッチ、履歴が同じ公開結果を示すことを確認した。サーバーの勝敗・能力・追加札の決定経路は変更していない。
- 検証: `node scripts/check-syntax.js` 成功。効果分類・表示イベント・PvP UI契約・ゲームルールの44件が成功。
- 残るリスク: 自動ブラウザのviewport overrideが実際の描画幅へ反映されず、360×800／390×844の厳密な手動視覚回帰は未実施。CSS契約と固定幅の回帰テストは維持するが、実デバイスまたは正常なviewport環境で後続確認が必要。
- 次: 入口／Random／観戦の状態遷移を実画面で再確認し、拡張の対象選択と再接続を含むラウンドストーリーを段階的に整備する。

### 2026-09-09 — Phase 4 Random／観戦フロー確認

- 状態: 実装済み経路を隔離ローカルで確認。
- Random: 検索中の人数・中止操作、二人成立後の固定ルールと開始同意、片方がホームへ戻った直後の「別の相手を自動で探しています」表示と再検索への復帰を確認した。
- 観戦: 拡張Privateの対局へ観戦入室し、♠／♥側と名前・得点・両手札、対局結果、使用中Tarot、観戦者数、退出、空席参加予約を確認した。観戦画面にはカード確定・降参・設定編集の操作が出ない。
- 残るリスク: 認証済みRanked、実デバイス幅、切断後10秒の復帰と対象選択中再接続は、この隔離画面では未確認。既存の自動テスト契約を通したうえで、対応環境で継続確認する。

### 2026-09-09 — Phase 0／Phase 2 公開結果の一回性・アクセシビリティ

- 状態: 最初のラウンドストーリー改善単位を完了。`reveal-area` は新しい公開ラウンド、または新たに公開された対象効果だけを `aria-live` で通知し、初期復元・チャット・参加状態・再接続による同じ結果の再描画では既存DOMを保つようにした。
- 追加: 最終結果も、実際に新しく到着した終局だけを `role=status` として通知する。復帰後の結果は見出し付きの通常領域として残すため、既に知っている勝敗で操作を中断させない。投了時は終局パネルだけを読み上げ、盤面側との重複を避ける。
- 追加: 「選択中のカード」も、プレイヤーが新しく札を選んだときだけ能力を通知する。同じ札を考え続けている間のルーム更新では、詳細DOMを作り直さず、キーボード操作・表示位置・選択状態はそのまま保つ。
- 境界: 描画キーはラウンドID、既に公開済みの効果識別、観戦／座席、持ち越し数、終局種別だけから作る。未公開札、Noiseの実体、候補、nonce、内部カードIDは保持・参照しない。
- 配信: `main.js` の更新を確実に受け取れるよう、PvP読み込みチェーンを `pvp-v41` へ更新した。
- 検証: `node scripts/check-syntax.js` と `node --test pvp-ui.test.js presentation-events.test.js effect-language.test.js` が41件成功。続けて全体 `npm test` も成功（構文検査、Ranked値表生成、全Nodeテスト）。追加した契約は、既存公開結果のDOM保持、初期hydrateの静かな表示、新規公開結果／終局だけのライブ通知、終局優先を固定する。
- 実画面: 新しいローカルPrivateルームで、A側がAceを選ぶと詳細欄が `aria-live=polite` になること、B側のカード確定によりルーム更新が届いても選択内容と表示位置が変わらないこと、A側で選択を解除すると詳細欄が非表示・`aria-live=off` へ戻ることを確認した。
- 実画面: 同じ対局でAce対Kingを公開し、結果欄が新しいラウンド時だけ `aria-live=polite` となり、サーバー確定の「強さ 14 対 13 を比較」と獲得2枚を表示することを確認した。
- 残るリスク: スクリーンリーダー実機での発話順序、対象選択中の再接続、360×800／390×844の実レンダリングは、このブラウザ環境では未確認。次の実画面検証では、対象選択の行為者／相手投影とともに確認する。

### 2026-09-09 — Phase 4 Ranked 結果通知の分離

- 状態: 一部完了。既存の「最終スコア／判断成績／選択損失／レーティング／結果運」という情報分離を、実結果・判断評価・レート対象の日本語ラベルと補足で明確化し、復帰・再取得のたびに同じラウンド結果や終局を読み上げ直す問題も解消した。
- 追加: Rankedのラウンド結果と最終結果は、初期復元と同一ゲームの再取得ではラベル付きの静かな領域として保持する。新しいラウンドだけを `role=status` として通知し、終局は排他的な新規結果として一度だけ通知する。
- 追加: 終局画面の指標には「最終スコア（実結果）」「判断成績（レート対象）」「選択損失（低いほど良い）」「結果運（実結果との差）」を明記し、レートは判断成績を基準に更新することを短い補足で示した。
- 配信: `ranked.html` の関連読み込みを `ranked-v13` へ更新し、古いブラウザキャッシュが `ranked-client.js` を残さないようにした。
- 検証: `node scripts/check-syntax.js` と `node --test ranked-ui.test.js ranked-client.test.js presentation-events.test.js` が16件成功。ローカルのRanked利用不可画面でも、ラウンド結果・最終結果がともに `role=region`／`aria-live=off` で初期表示されることを確認した。
- 残るリスク: 認証済みの実Ranked完走、終了後にプロフィール／ランキングを再取得した際の支援技術の実発話順は、認証・DBを設定した隔離環境が必要なため未確認。

### 2026-09-09 — Phase 3 対象操作の一回性・入力状態

- 状態: 完了（対象選択の操作フィードバック単位）。既存のサーバー権威な候補・nonce・期限・確定経路は変えず、`private-action-panel` を初期状態では静かなラベル付き領域とした。
- 追加: 新しい対象操作だけを `role=status`／`aria-live=polite` とし、同じ操作の再描画、チャット更新、再接続復元では既知の指示を読み上げ直さない。自分が盤面の候補を選んだときだけ、選択内容と確定手順を一度通知する。
- 追加: 通常の手札選択と能力の対象選択に `aria-pressed` を付けた。視覚の発光・チェックは維持し、マウス、タップ、キーボード、支援技術で「選択済み」を同じ意味で確認できる。
- 秘匿境界: 表示キーは行為者に既に投影済みの操作ID、または非行為者には期限・phaseだけを用いる。nonce、候補一覧、発動元、Noiseの実体、対象札の正体を共有イベントやDOM属性へ追加していない。対象名を読むのは、サーバーが候補を許可した行為者のローカル表示だけである。
- 検証: `node scripts/check-syntax.js`、`node --test pvp-ui.test.js presentation-events.test.js private-room-view.test.js` は42件成功。`git diff --check` 成功。さらに本作業中の全体 `npm test`（構文検査、Ranked値表生成、全Nodeテスト）も成功した。
- 配信: `main.js` の更新を確実に受け取るよう、PvP読み込みチェーンを `pvp-v42` へ更新した。
- 残るリスク: 実機スクリーンリーダーの発話順、実ターゲット操作を含む再接続、360×800／390×844の実レンダリングは隔離環境で未確認のまま残す。候補とnonceの受信者別投影は既存自動テストで継続固定する。

### 2026-09-09 — Phase 0 状態案内の重複抑止

- 状態: 完了。待機・選択・相手待ち・再接続・エラーを示す既存の状態バナーは維持し、文言と意味別stateが同じならDOMを書き換えないようにした。
- 追加: ルームのpresence、チャット、再接続更新で同じ「相手の選択を待っています」等を繰り返しライブ通知しない。意味が変わったときだけ表示・読み上げを更新し、新しい対局scopeでは同じ文言も正しく一度案内できる。
- 配信: PvP読み込みチェーンを `pvp-v43` へ更新した。古い `main.js` が残ることによるARIA挙動の不一致を避ける。
- 実画面: `NODE_ENV=development` の標準ローカルOrigin `http://localhost:3000` で、入口・モード説明・ルール早見表が接続済みで表示されることを確認した。本番用Origin制限を緩める変更はしていない。
- 検証: `node scripts/check-syntax.js` と `node --test pvp-ui.test.js presentation-events.test.js` が37件成功。対象選択・秘匿投影まで含めた37件の直前検証、および本作業中の全体 `npm test` は成功。最終統合前にキャッシュ版更新を含む全体テストを再実行する。

### 2026-09-09 — Phase 2 主操作チェックポイント／対象トレイの静音化

- 状態: 完了（対局操作の小さな整流単位）。確定ボタンは、カード未選択時に「カードを選んでください」、選択後に「この一枚で勝負する」、サーバーが伏せ札を受理した後に「カードを伏せました」と表示する。通信断・能力対象選択中も誤った確定操作を示さず、すべて既存のルーム投影とローカル未送信選択からのみ導出する。
- 追加: 対象札の候補トレイは `aria-live=off` にし、新しい能力操作の案内は既存の中央パネルだけで一度通知する。候補札のラベル・`aria-pressed`・確定導線は保つため、候補一覧の再描画が支援技術を繰り返し中断しない。
- 境界: カード名、確定可否、対象候補、効果、勝敗を新たにクライアントで判断していない。未公開選択、Noise、nonce、受信者別候補の通信・DOM境界は変更していない。
- 配信: PvP読み込みチェーンを `pvp-v44` へ更新した。
- 検証: `node scripts/check-syntax.js` と関連UIテスト42件が成功。続けて全体 `npm test`（構文検査、Ranked値表生成、Private／Ranked／認証／UIを含むNodeテスト）が成功した。ローカル入口を新しい読み込みチェーンで再表示した。
- 残るリスク: 実対局での画面サイズ別の視覚回帰、実機スクリーンリーダーの発話順、認証済みRanked、対象選択中の再接続は、対応する隔離環境または実デバイスでの継続検証が必要。

### 2026-09-09 — Phase 3 Tarot継承／Star操作とモバイル説明

- 状態: 完了（機能修復と可読性の小単位）。The Hermitは、過去ラウンドの解決済み数値を反響する旧挙動をやめ、The Foolと同じく相手の直前の実カードとしてこの局面で振る舞う。物理札はHermitのまま残し、比較・能力だけを継承先のカードとしてサーバーが解決する。Blank／Fool／Hermitを再帰参照した場合の強さ0・能力なしは維持した。
- Star: 仕様どおり「敗北時」だけに起動することをカード説明・対象操作パネル・追加候補トレイで明示した。サーバー統合テストで、敗北後に相手だけへ凍結済み候補を投影し、選択した札を一回だけNoise生成し、非所有者へ定義・能力を漏らさないことを固定した。
- モバイル: 選択中カードの説明をカード名・強さと別の全幅行にし、親要素の切り抜きを解除した。使用中Tarotの詳細も、狭い幅で名前・説明が折り返されるようにした。能力説明は内容を省略せず、画面を縦に伸ばして読める設計とする。
- 文書: `PRIVATE_PVP_REMAINING_TAROT_SPEC.md` を、実装済みのHermit／Foolの実カード継承仕様へ更新した。旧`echoProfile`は過去対局の互換情報として残し、現在の継承判定には使わない。
- 配信: PvP読み込みチェーンを `pvp-v45` へ更新し、CSS・クライアントの古いキャッシュが残らないようにした。
- 検証: `node scripts/check-syntax.js`、関連Nodeテスト89件、`git diff --check`、全体 `npm test` が成功。対象はHermitの局面再評価、Starのサーバー処理・一回性・秘匿投影、選択カード／Tarot説明のモバイルCSS契約、キャッシュ版を含む。
- 残るリスク: 本ツールのviewportを実デバイス幅へ確実に固定できなかったため、360×800／390×844の実スクリーンショット比較は未実施。CSSの小幅レイアウト契約とDOMの可読性は自動テストで固定したが、公開後は実機でも一度確認する。

### 2026-09-09 — Private拡張の開始待機・効果選択・可変ラウンド上限

- 状態: 完了（報告された操作摩擦とカード効果の修復）。開始同意は、両者の準備状況を一つの読みやすい状態パネルにまとめ、同意・観戦切替の主操作は途中で不自然に改行しない最小幅と折り返し契約を持たせた。大型画面では履歴とチャットを別列へ分離し、対局の閲覧と会話を同時に追える。
- Star: `The Star` は勝敗後ではなく、出したラウンドの終了後に常に相手へ一回だけ対象選択を提示する。相手が選んだ凍結デッキ内の札だけを、その相手の手札へNoiseとして加える。終局時は従来どおり対象選択を発生させず、結果パネルを妨げない。
- High Priestess: 相手手札の候補を、行為者にだけ投影済みの同じ物理札IDで盤面上から選べるようにした。対象札は通常の札選択と混線しないネイティブbuttonとし、タップ・キーボード・選択状態のいずれでも確定導線へ進める。
- ルールと時間: 拡張Privateの総ラウンドは、初期デッキ枚数を超えても追加・複製を見込んで最大10ラウンドまで指定でき、全体の絶対上限は24とする。Guest PvPの第1ラウンドだけは、サーバー権威で選択済み制限時間に30秒を足す。Rankedの別仕様タイマーは変更しない。
- 可読性: デッキ一覧の高さは邪魔な増減ボタンを廃止し、180〜440pxのコンパクトなスライダーに置き換えた。効果キューは `✦ 追加・複製` を途中で改行せず、結果エリアは固定高を廃止して複数効果が縦にはみ出さず読める。
- 秘匿境界: StarのNoise実体、対象候補、nonceを相手・観戦者・効果表示へ追加していない。High Priestessの候補IDは既に行為者へ許可された相手手札のみに限り、対象操作のサーバー権威・一回性・期限を維持する。
- 検証: `node --test pvp-ui.test.js server.test.js private-deck.test.js private-ruleset.test.js private-advanced-tarot.test.js private-room-view.test.js private-action-queue.test.js` は98件成功。続けて `npm test`（構文検査、Ranked値表生成、全Nodeテスト）は267件成功。上限、Starの勝敗非依存・秘匿性・一回性、High Priestessの物理札ID、初回+30秒、表示CSS契約を回帰テスト化した。
- 実画面: ローカル入口のDesktop DOM・操作要素と横幅を確認した。今回の隔離ブラウザではSocket.IOの実接続が確立せず、待機中／対象選択中の実スクリーンショット比較は未完了と記録する。一方、実Socket.IOを用いる既存統合テストは入室、開始、確定、再接続、観戦予約を通過している。
- 残るリスク: 360×800／390×844の実機で、長いTarot名・同時効果・第1ラウンドのタイマー表示を確認する必要がある。今回の可変高・非折返し・小幅CSS契約は自動テストで固定したが、公開後の実機確認を推奨する。
