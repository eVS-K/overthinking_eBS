# Private PvP 拡張 — 効果・ルール説明・カード表示の是正設計

最終更新: 2026-09-06
状態: **実装・検証済み（2026-09-06）。下記「実装済みの決定」を正本とし、将来の機能追加時には同じ境界とテストを維持する。**

## 0. 目的と適用範囲

`private-expanded-v1` の能力カードを、初見でも理解でき、対局中に安全かつ
見失いにくく使えるものにする。対象は Private PvP の拡張デッキだけである。

- 通常Private、ランダムマッチ、ソロ、Ranked、`game-rules.js` の勝敗規則は変えない。
- カードの勝敗、得点、対象候補、時間切れの選択、効果結果は従来どおりサーバー権威とする。
- ノイズ札の正体は、CSS・DOM・Socket payload・履歴のどこからも所有者以外へ漏らさない。
- 改善は「見た目だけ」で終えず、効果の順序、終了判定、再接続、観戦、アクセシビリティ、
  テストまで同じ設計へ揃える。

この文書は既存の
`PRIVATE_PVP_REMAINING_TAROT_SPEC.md` を補完する。カード効果そのものの正本は
同文書と純粋エンジンであり、本書は **提示方法・対象選択・終了時の扱い・視覚体系** の
正本である。

## 実装済みの決定（2026-09-06）

以下は設計だけでなく、現在のランタイムと回帰テストへ反映済みである。

- `private-card-definitions.js` の不変メタデータから、現在の拡張デッキに含まれる概念だけを
  `activeConcepts` としてサーバーで生成し、「この部屋のルール」に表示する。
- 対象選択は候補ボタン一覧を廃止した。手札対象はその手札上で、追加候補と獲得札対象は
  対象側ゾーン内の一時的なカード列で選び、最後に明示的な確定ボタンを押す。
- 対象操作の時間は `PRIVATE_ACTION_TIMEOUT_MS = 30_000`。終了判定が先に成立したラウンドは、
  未解決の対象操作を監査可能な `skipped-target-action` として記録し、選択UIを出さない。
- 効果履歴の公開投影は、対象seatを必須にして曖昧な効果をfail closedにする。Moonの獲得札破棄も
  両参加者・観戦者に同じ公開結果として見える。ノイズ札の定義・能力・内部状態は引き続き
  所有者以外へ送らない。
- 生成札はサーバーの閉じた `state.generated === true` と発行台帳で追跡する。通常の生成札は
  小印とグリッチ表現を持ち、ノイズ札はそれとは別の匿名・破線表現を使う。
- Tarotの `visualRole` は条件変動・ロック・複製/追加・その他・Emperorに分類し、色だけでなく
  デッキ一覧の役割ラベルにも出す。The Emperorは金の専用表現である。
- 拡張デッキ一覧はフェード/スクロール案内に加え、180–440pxの範囲で縦方向のみ高さを調整できる。
  この画面上の好みはセッション単位で保存し、部屋のゲーム設定には混ぜない。
- 効果演出はラウンドIDだけでなく、公開済み効果の安全な識別子でも一度だけ発火する。これにより、
  対象選択を確定した**後**に同じラウンドのviewが更新されても、複製・ロック・破壊等の演出を取り逃さない。
- `npm test` は構文検証を必ず先に実行する。さらに、カード実体・対象操作キュー・受信者別room view・
  ルール概念カタログには個別のcoverage gateを設けた。Sunの実Socketテストも、対象候補が当事者だけの
  `target.cards` / `target.candidateIds` にネストされ、相手へ漏れないことを回帰検証する。

ローカル実ブラウザでは、デスクトップと390px幅の画面で、開始同意、選択時の能力表示、30秒の対象操作、
盤面候補選択、確定後の複製演出、両者への結果投影を確認した。現時点では、正規化済みの公開
`record.effects` を全員の履歴と結果表示の共通ソースとし、ノイズの詳細だけを既存の受信者別カード投影で秘匿する。

## 1. 現状から確認できた問題と根本原因

| 問題 | 現在の原因 | 是正方針 |
| --- | --- | --- |
| 新概念の説明がない | ルームルールはデッキ構成を説明するだけで、カード能力の概念を収集していない | カード定義の概念タグから、その部屋だけの用語集をサーバーで生成する |
| 対象が場の札ではなく下の候補ボタン | `private-action-candidates` が候補の唯一の表示面 | 候補を対象プレイヤーの手札・獲得札・生成候補カード列へ描画し、カードを選択してから確定する |
| 効果選択が20秒 | `PRIVATE_ACTION_TIMEOUT_MS` とタイマーの上限が20秒 | 対象操作は30秒へ統一する。通常ターンと効果操作の時計を明確に分ける |
| 終局後にも対象選択が発生する | 対象操作キューを完了してから終局判定している | 自動効果後・任意の対象選択前に終局を判定し、終局なら対象操作を安全に失効させる |
| 効果結果が片方だけに見える | 効果の結果通知が「履歴の副産物」であり、受信者別の結果イベントとして扱われていない | 効果ごとに公開範囲を持つ server-side effect event を作り、全員へ同じ順序で投影する |
| ノイズ札の見た目が弱い | 破線と灰色のみで、状態・追加演出・機密性の境界が視覚化されていない | ノイズ専用の匿名カード表現と、生成札とは別のアニメーション体系を持たせる |
| 生成札の増加が分かりにくい | 生成元・生成タイミングを表示モデルへ渡していない | 生成札の由来を状態として持ち、永続的な小印と一度だけのグリッチ演出を分離する |
| Tarotの色が一律 | 色が `category: tarot` のみで決まる | `visualRole` をカード定義へ追加し、意味別の控えめな色系統にする |
| 長いカード名が中央記号を圧迫する | 上部名にカード面専用の短縮名・幅制約がない | 顔用短縮名、固定グリッド、縮小済み文字サイズ、ellipsisを導入する |

## 2. 固定する用語

「コピー」は二つの意味を持つため、プレイヤー向けの文章では次のように固定する。

| 用語 | 意味 | 使うカード・画面 |
| --- | --- | --- |
| **能力をコピー** | 強さ・安全な比較能力だけを反響する。新しい札は作らない | The Fool / The Hermit |
| **カードを複製** | 同じ定義の新しい物理札をサーバーが発行する。元札のロック・ノイズは引き継がない | Magician / High Priestess / Hierophant / Judgement / The World など |
| **カードを追加** | 定義が決まった新しい物理札を手札へ発行する。対象札を複製しない場合にも使う | Lovers / Hanged Man / The Star |
| **ロック** | 次の対象プレイヤーの解決済みラウンド終了まで、該当札を選べなくする | Empress / Justice |
| **破壊（破棄）** | 札を破棄札へ移し、この対局中は戻さない | The Sun / The Moon / The World |
| **ノイズ** | 所有者だけが正体を見られる札。出すまで他人には名前・強さ・能力を送らない | The Star |

実装時は、以下のプレイヤー向け文字列を一括で監査する。

- `private-card-definitions.js` の `desc`
- `private-room-view.js` の対象指示文
- `main.js` の効果履歴・結果文
- ルームルールの動的用語集
- `PRIVATE_PVP_REMAINING_TAROT_SPEC.md` の利用者向け説明

内部の変数名・既存保存済み履歴の `copy-*` 型名は互換性のため残してよいが、
新しい利用者向け文字列で「札のコピー」を使わない。

## 3. データ定義の拡張

### 3.1 概念タグ

カード説明の文字列から概念を推測してはいけない。`private-card-definitions.js` の
各定義に、表示専用・不変のメタデータを追加する。

```js
{
  id: 'the-high-priestess',
  // 既存の effectProfileId / requiresFeatures はゲーム実装用のまま残す
  ruleConceptIds: ['card-duplication', 'target-selection'],
  visualRole: 'generation',
  faceLabel: 'High Priestess'
}
```

`ruleConceptIds` は説明の表示だけに使い、クライアントが能力を有効化する権限にはしない。
`visualRole` も見た目だけで、勝敗や対象候補の正本にしてはならない。

### 3.2 ルール概念カタログ

新規 `private-rule-concepts.js` を作る。責務は「凍結済みデッキから表示してよい用語集を
決定する」ことだけであり、SocketやDOMを持たない。

```js
const PRIVATE_RULE_CONCEPTS = Object.freeze({
  'ability-echo': {
    order: 10,
    title: '能力をコピー',
    description: '直前ラウンドで確定した強さと、安全な勝敗判定能力だけを使います。札は増えません。'
  },
  'card-duplication': {
    order: 20,
    title: 'カードを複製',
    description: '指定された札と同じ種類の新しい札を手札へ作ります。元札の状態は引き継ぎません。'
  },
  'card-addition': {
    order: 30,
    title: 'カードを追加',
    description: '能力で決まった種類の新しい札を手札へ加えます。手札・総札の上限では増えない場合があります。'
  },
  lock: {
    order: 40,
    title: 'ロック',
    description: 'ロックされた札は次の対象プレイヤーのラウンド終了まで選べません。Blankが有効なら代わりに選べます。'
  },
  'destroy-discard': {
    order: 50,
    title: '破壊・破棄',
    description: '対象札を破棄札へ移します。破棄された札はこの対局中には戻りません。'
  },
  noise: {
    order: 60,
    title: 'ノイズ',
    description: 'ノイズ札の正体は所有者だけに見えます。出した時点で全員へ公開されます。'
  },
  'round-limit-change': {
    order: 70,
    title: '総ラウンド数の変更',
    description: '能力により、この対局の残りラウンド数が増減することがあります。'
  },
  'tarot-negation': {
    order: 80,
    title: 'Tarot効果の無効化',
    description: 'The EmperorはTarotとの勝負で優先し、対象Tarotのこのラウンド由来の効果を無効化します。'
  },
  'won-pile-operation': {
    order: 90,
    title: '獲得札への操作',
    description: '能力が獲得済みの札を破棄または参照することがあります。得点は残っている獲得札の枚数です。'
  }
});
```

想定するカードごとの概念タグは次の通りとする。

| 概念 | 該当カード |
| --- | --- |
| `ability-echo` | The Fool, The Hermit |
| `card-duplication` | Magician, High Priestess, Hierophant, Judgement, The World |
| `card-addition` | Lovers, Hanged Man, The Star |
| `lock` | Empress, Justice |
| `destroy-discard` | The Moon, The Sun, The World |
| `noise` | The Star |
| `round-limit-change` | Wheel of Fortune |
| `tarot-negation` | The Emperor |
| `won-pile-operation` | The Moon, The World |

### 3.3 ルームルールへの投影

`getPublicRoomRules(room)` が、凍結済みの `deck` と `blankEnabled` から
`activeConcepts` をサーバーで生成して返す。

```js
{
  ...existingPublicRules,
  activeConcepts: [
    { id: 'lock', title: 'ロック', description: '…' },
    { id: 'card-duplication', title: 'カードを複製', description: '…' }
  ]
}
```

- クラシック・ランダム・ソロ・Rankedは空配列とする。ホームの標準ルール説明も変えない。
- 拡張デッキで該当札が0枚なら、その用語を出さない。
- 対局中、観戦中、待機中で同じスナップショットを出す。
- 画面の「この部屋のルール」詳細内へ `このデッキで使う能力` として挿入する。
- 追加した概念は色だけで伝えず、見出し・本文を常に表示する。

## 4. 対象選択の操作設計

### 4.1 基本操作

候補ボタン一覧を廃止し、**盤面上のカードを選択し、その後に確定**する二段階操作へ
統一する。

1. 効果バーが「誰の、どの場所から、何を選ぶか」と30秒タイマーを表示する。
2. 候補のカードだけが盤面で発光し、カードを押すと `対象として選択中` の印が付く。
3. 効果バーの `この札に効果を適用` が有効になる。押すまでサーバー状態は変えない。
4. 確定時だけ `actionId` / `nonce` / `gameRevision` / `target` を送信する。
5. 成功後、カード演出と結果イベントを表示して次の処理へ進む。失敗時は最新viewを要求し、
   対象選択を安全にリセットする。

二段階にする理由は、狭い画面での誤タップと、The Worldなど取り消せない効果の誤適用を
避けるためである。キーボードでは、候補カードへTab、Enter/Spaceで選択、効果バーの
確定ボタンへTab、Enterで確定できる。Escapeは未確定の表示選択だけを解除する。

### 4.2 対象面の対応表

| 効果 | 選択する場所 | UI |
| --- | --- | --- |
| The Sun | 使用者自身の手札 | 現在の手札カードを候補化 |
| High Priestess | 相手の公開手札 | 相手手札カードを候補化。ノイズ札は候補にしない |
| Hierophant | 使用者自身の手札 | 自分手札カードを候補化 |
| Justice | 相手手札 | 相手手札カードを候補化。ノイズ札は匿名表示のまま候補にできる |
| Hanged Man / The Star | 追加を受ける側のカード列 | 一時的な「追加候補カード列」を、そのプレイヤーの手札直下へ出す |
| The World | 相手の獲得札 | 相手ゾーンに一時的な「獲得札一覧」を展開し、対象だけ候補化 |

Hanged Man / The Star の候補は既存の物理札ではなく、凍結デッキから作った**追加候補**である。
したがって、候補カード列には「この札を追加する」と明記する。候補がStar由来でも、
選ぶ本人には通常札の情報を見せる一方、他人には候補列・正体とも送らない。

### 4.3 表示コンポーネントの責務

現在の `createCard(card, suitType, isInteractive)` は通常の手札選択と能力対象選択を
兼任させない。次の描画コンテキストを導入する。

```js
createCard(card, suitType, {
  interaction: 'none' | 'play-card' | 'effect-target',
  targetSelected: false,
  targetActionId: '',
  generatedVisual: 'none' | 'persistent' | 'new',
  roleLabel: ''
});
```

- `play-card` は現行の手札選択のみを扱う。
- `effect-target` は通常のカード選択・確認ボタンを変更せず、
  `privateTargetSelectionId` だけを更新する。
- ロック札もJusticeの対象にはなり得るため、`effect-target` は通常の
  `isLocked` によって機械的に無効化しない。候補可否はサーバーの候補配列だけで決める。
- 候補でないカード、非当事者、観戦者はクリック不能である。
- ノイズ札は所有者以外では常に匿名カードのまま描画する。候補であっても
  `definitionId` や能力文をDOMへ入れない。

候補リスト用の `private-action-candidates` は廃止する。`private-action-panel` 自体は
「効果名、説明、残り時間、確定ボタン」だけを持つ小さな効果バーとして残す。

### 4.4 30秒の時間契約

`PRIVATE_ACTION_TIMEOUT_MS` を `30_000` へ変更し、生成・検証・再接続・UI表示に
同じ定数を用いる。クライアントは演出用の値を独自に持たない。

- **post-result**: 通常ターンを開始する前に30秒を与える。候補がある限り、時間切れでは
  サーバーが暗号学的乱数で1つを選ぶ。
- **pre-commit (The Sun)**: Sunを確定した使用者に30秒を与える。相手にはSun選択中の
  事実を見せず、相手の通常ターン時計は元の期限のまま進む。
- Sunの対象操作が通常ターンの残り時間より長くても、相手の時計を延長しない。
  相手が先に時間切れなら既存のサーバー選択が行われ、Sunの対象確定後に通常どおり
  同時解決へ進む。
- 切断時は残り時間を保存し、10秒の再接続猶予後に期限処理へ戻る。再接続で30秒へ
  リセットしない。

## 5. 終局時に任意対象操作を行わない状態遷移

### 5.1 新しい確定順序

対象を選ばせる効果は「このラウンドを終局から覆す」ために使ってはならない。新しい順序は
以下とする。

```text
1. pre-commit target             The Sun
2. simultaneous commit / reveal  両者のカードを確定
3. comparison / canonical award  勝敗、獲得、スタック
4. automatic effects             Moon、Empress、生成、Wheelなど、選択不要の効果
5. terminal checkpoint           得点・手札尽き・有効ラウンド上限を判定
6. queued target actions         5で未終局の場合だけ処理
7. final bookkeeping             次ラウンドまたは最終結果
```

第5段階で終局していた場合は、未解決の対象操作を実行しない。

- 各未解決操作を履歴へ `skipped-target-action` として残し、理由を
  `game-ended-before-target-selection` とする。
- クライアントには候補・nonce・対象選択パネルを一切送らない。
- 最終結果には `試合終了のため、残りの対象選択は行いません` と一度だけ表示する。
- 対象操作によるカード追加・ロック・移送・得点変更は終局後に起こらない。

これは任意選択に限る。Moon、Empress、Magician、Lovers、Wheelなど、既にラウンドの
必須自動処理として定義されたものは第5段階より前に完了する。

### 5.2 実装境界

純粋エンジンへ、対象キューを発行した直後の終局を安全に確定できる関数を追加する。

```js
getAdvancedRoundDisposition(state) // { terminal, terminalReason }
skipAllAdvancedTargetActions(state, 'game-ended-before-target-selection')
```

`server.js` は `advancePrivateActionQueue` を呼ぶ前にこの結果を見る。
終了時のスキップもエンジンで検証可能な履歴にするため、Socket層だけで配列を空にしてはならない。

## 6. 効果結果の公開・ノイズの秘匿

### 6.1 効果結果イベント

履歴文字列だけに依存せず、サーバー内部で一回限りの `effectEvent` を構築する。

```js
{
  id: 'room-revision:round:effect-index',
  round: 4,
  kind: 'lock' | 'duplicate' | 'add' | 'destroy' | 'noise' | 'discard' | 'round-limit',
  sourceSeat: 'p1',
  targetSeat: 'p2',
  visibility: 'public' | 'owner-only-detail' | 'anonymized',
  publicText: 'μ The Empress：♥側の札を3枚ロック',
  ownerText: null,
  animation: 'lock-burst'
}
```

`effectEvent` は生のカード実体・未公開定義・nonceを含めない。各viewerには
`private-room-view.js` で投影したものだけを渡す。

| 効果 | 使用者 | 対象所有者 | 相手・観戦者 |
| --- | --- | --- | --- |
| ロック、公開札の複製、公開札の追加、破壊 | 同じ公開結果 | 同じ公開結果 | 同じ公開結果 |
| Starのノイズ追加 | 「ノイズ札を追加」と表示 | 選んだ札名と「ノイズとして追加」を表示 | 「ノイズ札を追加」のみ |
| Sunが所有者のノイズ札を破壊 | 札名を表示してよい | 同左 | 「手札を1枚破棄」のみ |
| Justiceがノイズ札をロック | 「匿名のノイズ札をロック」 | 同じ匿名結果 | 同じ匿名結果 |

過去履歴も同じ投影関数を通す。`publicExpandedRoundEffect` のような全員共通の
オブジェクトを直接再利用して、ノイズの詳細を渡してはいけない。

### 6.2 演出の優先順位

同時に複数効果が生じても、画面を点滅させ続けない。効果イベントを安定順で最大3件ずつ
提示し、それ以上は履歴へ集約する。

1. 破壊・破棄: 対象札が短く崩れて破棄方向へ消える
2. ロック: 対象札に輪郭の収束と鍵のスナップ
3. 複製・追加: 新札がグリッチから実体化
4. ノイズ: 匿名カードがスキャン線と乱れを伴って現れる
5. 得点・ラウンド数: スコア/ラウンド表示が一度だけ強調される

`prefers-reduced-motion: reduce` では、移動・グリッチを止め、色・アイコン・ライブテキスト
だけで同じ情報を伝える。

## 7. カードの見た目体系

### 7.1 Tarotの視覚ロール

`visualRole` は次の5種に固定する。色だけに頼らず、デッキ一覧・選択詳細には役割ラベルも出す。

| visualRole | 対象 | 表現 |
| --- | --- | --- |
| `conditional` | Death, Temperance, Devil, Tower, Chariot, Strength, Fool, Hermit | 現在の紫に近い青紫。細い脈動線と `変動` ラベル |
| `lock` | Empress, Justice | 冷たい藍青。鍵形の細い枠と `ロック` ラベル |
| `generation` | Magician, High Priestess, Hierophant, Lovers, Hanged Man, Star, Judgement, World | 青緑ではなく淡いシアン〜青紫。`複製・追加` ラベル |
| `other` | Wheel of Fortune, Moon, Sun | 現行に近い紫。`特殊` ラベル |
| `emperor` | The Emperorのみ | 深い黒紫に金の枠・控えめな放射。`Tarot無効化` ラベル |

The Emperorは金を勝利・選択の金と混同しないよう、カード面の内枠・中心記号・デッキ行だけに使う。
通常の選択状態は従来どおり明るい金の輪郭を優先する。

### 7.2 Joker / 3 / 2 の穏やかな能力色

Joker、3、2は「能力あり」の緑枠を維持するが、カード本体は能力なし札の青に寄せる。

- 枠: 現行に近い `#8ccbb7` を維持する。
- 本体: 青い `card-no-ability` の背景を基準にする。
- 重ね色: 現行の濃い緑を、低彩度・低不透明度の青緑オーバーレイへ下げる。
- デッキ一覧: 同じく青ベース + 薄い青緑。Tarotの `generation` 色とは別のトークンを使う。

色トークンは次のように集中管理する。個別カードのCSSで色を再定義しない。

```css
:root {
  --card-ability-border: #8ccbb7;
  --card-ability-overlay-start: rgba(80, 151, 142, .10);
  --card-ability-overlay-end: rgba(58, 119, 125, .06);
  --tarot-conditional: ...;
  --tarot-lock: ...;
  --tarot-generation: ...;
  --tarot-other: ...;
  --tarot-emperor: ...;
}
```

### 7.3 ノイズ札と生成札を分ける

| 状態 | 意味 | 見た目 | 情報境界 |
| --- | --- | --- | --- |
| `card-noise` | 他人に正体を伏せる札 | 破線、`?`、走査線、断続的な短いノイズ。中身を連想させる文字を出さない | 所有者以外は名前・能力・強さなし |
| `card-generated` | 能力で新しく発行された公開札 | 通常の札面を保ち、角の `＋` 印と一度だけのグリッチ実体化 | 公開札なら全員に表示 |
| `card-generated-noise` | Starで生成された未公開札 | ノイズ札を優先して描画する。生成グリッチは正体公開時まで抑制する | 他人に生成元・定義を出さない |

生成札にはサーバー発行の `origin` を持たせる。

```js
origin: {
  kind: 'initial' | 'generated',
  effectEventId: 'stable server-side id',
  createdRound: 4
}
```

`origin` はクライアント入力から作らない。公開viewには、見せてよい場合だけ
`state.generated: true` と `generationEventId` を投影する。クライアントは最初の
room hydrate時に既存イベントを既読化し、その後に届いたIDだけを一度アニメーションする。
再接続・再描画でグリッチが無限再生されないようにする。

## 8. レイアウトと文字組み

### 8.1 拡張デッキ一覧の高さ

一覧の高さは個人の表示設定であり、対局ルール・プリセット・サーバー保存値には含めない。

- `input[type=range]` を `拡張デッキ一覧の高さ` として追加する。
- 範囲は 220px〜560px、20px刻み、初期値360px。
- CSS変数 `--expanded-deck-list-height` だけを変える。幅・列数は変えない。
- 値は `localStorage` に安全な整数として保存し、範囲外・壊れた値は初期値へ戻す。
- モバイルは `min(選択値, 56dvh)` を上限とし、設定が画面全体を占有しないようにする。
- フェードと「下へスクロール」は高さ変更・ResizeObserver後にも再計算する。

### 8.2 長いカード名

カードの物理サイズは手札枚数により変えない。文字だけを安全に収める。

1. 定義に `faceLabel` を追加する。例: `The High Priestess` のカード面左上は
   `High Priestess`、完全名はARIA名・選択詳細に残す。
2. `card-top` を `minmax(0, 1fr) auto auto` のグリッドにし、名前要素に
   `min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap` を与える。
3. カード中央のギリシャ記号は、上部名の幅に影響されない絶対中央を維持する。
4. 上部文字・中央記号・隅のpipをそれぞれ少し縮める。カード本体の幅を広げて
   レイアウトを崩す方法は使わない。
5. `title` 属性とARIA名には省略前の正式名を入れ、選択詳細で常に全文と能力を読めるようにする。

最低確認幅は desktop 1280px / 1440px と mobile 360px / 390px とし、全22枚のTarotを
各手札に置いても横方向のページスクロール、中央記号との重なり、カード高の変化がないことを確認する。

## 9. 変更予定ファイル

| ファイル | 変更内容 |
| --- | --- |
| `private-card-definitions.js` | `ruleConceptIds`、`visualRole`、`faceLabel`、利用者向けの「複製」表記 |
| `private-rule-concepts.js`（新規） | 用語集カタログと、凍結デッキからの概念収集 |
| `private-card-instances.js` | 生成由来 `origin` の安全な正規化・複製・公開投影 |
| `private-game-engine.js` | 自動効果後の終局チェック、終局時の対象キュー安全スキップ、生成由来の不変条件 |
| `private-action-queue.js` | 30秒定数、対象面種別の正規化。候補自体の権威性は維持 |
| `private-room-view.js` | viewer別の効果イベント、対象面、ノイズを除いたカード投影 |
| `server.js` | 終局前のキュー停止、30秒タイマー、全員への効果イベント、獲得札対象面の投影 |
| `index.html` | 動的用語集、カード上の対象選択面、コンパクトな効果バー、一覧高スライダー |
| `main.js` | 対象カード選択状態、効果イベント消費、生成/ノイズ描画、動的ルール、用語統一 |
| `style.css` | Tarotロール色、Emperor金、穏やかな緑、ノイズ/グリッチ、カード文字組み、一覧高さ |
| `*.test.js` | 下記の回帰・境界・UI検証 |
| `PRIVATE_PVP_REMAINING_TAROT_SPEC.md` | 30秒、終局前チェック、用語、視覚状態の正式仕様 |

## 10. 実装順序

1. **純粋データと用語**
   - 概念タグ、visual role、face label、複製表記を入れる。
   - ルール概念収集の単体テストを先に作る。

2. **エンジンの終局境界**
   - 自動効果後・対象操作前の終局判定を実装する。
   - 終局時に対象操作がゼロ件になり、状態を変えないテストを作る。

3. **対象操作契約と受信者別view**
   - 30秒定数、target surface、effect event、ノイズ投影を実装する。
   - 通信再送・切断・観戦・二重送信・Star秘匿のテストを作る。

4. **盤面操作UI**
   - ボタン候補を撤去し、手札/獲得札/生成候補カード列へ移す。
   - 未確定選択と確定ボタン、キーボード操作、フォーカス復帰を実装する。

5. **視覚体系と演出**
   - Tarot色、Emperor、穏やかな緑、ノイズ、生成グリッチ、効果フィードを実装する。
   - `prefers-reduced-motion` の静的表示を実装する。

6. **デッキ一覧と文字組み**
   - 高さスライダー、保持、フェード再計算、長い名称の固定レイアウトを実装する。

7. **総合検証**
   - 下記の自動・実画面・再接続テストを通してから、最終的にドキュメントを更新する。

## 11. 必須テスト

### ロジック・通信

- デッキに該当カードがある時だけ、対応する概念説明が返る。
- カードを複製する能力と能力をコピーする能力の説明が混同されない。
- `PRIVATE_ACTION_TIMEOUT_MS === 30_000`、期限の再接続復帰・乱数選択・再送が一貫する。
- 終局したラウンドでは、対象選択が発行されず、対象効果で手札・score・stackが変化しない。
- High Priestess / Justice / Sun / World の候補が、正しい手札・獲得札だけに限定される。
- 非当事者、観戦者、古いnonce、他roomのactionId、二重確定は状態を変えない。
- 公開効果は両対戦者と観戦者で同じ順序・同じ公開文を受け取る。
- Starの候補、生成定義、ノイズ札の正体、Sunで破壊したノイズ札の正体が所有者以外へ漏れない。
- 生成札は上限・instanceId一意性・origin正規化を保つ。再接続で二重生成しない。

### UI・アクセシビリティ

- 対象候補は盤面カードとしてのみ操作でき、旧候補ボタン一覧が存在しない。
- カード選択→確定の二段階で、誤タップはサーバー状態を変えない。
- 自分/相手/観戦者でクリック可能な候補が正しく異なる。
- ロック・ノイズ・生成・Tarot役割が色以外のラベル/アイコン/ARIA名でも区別できる。
- 長いTarot名で左上表示がellipsisになり、中央記号・右上/右下のスートを覆わない。
- デッキ一覧の高さは縦だけ変わり、220〜560px以外にならず、モバイル上限も守る。
- reduced motion時、効果内容は読めるがグリッチ/移動が実行されない。

### 実画面の確認

- desktop: 1280×720、1440×900、フルスクリーン
- mobile: 360×800、390×844、縦/横切替
- 全22枚を含むデッキ、空きが少ないデッキ、Blank必須デッキ
- 高度効果を連続させた対局、終局直前の対象効果、Sunと相手の同時確定
- 所有者・相手・観戦者の3画面で、Starのノイズと効果フィードを比較
- Slow network相当、再接続中、対象選択期限、二重タップ

## 12. 変更時のリスクと防御

| リスク | 防御 |
| --- | --- |
| 盤面カードを候補にしたことで任意instanceIdを送れる | サーバーの候補配列・nonce・gameRevision検証を維持する |
| ノイズの候補カード表示で正体が漏れる | 候補表示も `private-room-view` の匿名カードだけを使う |
| 終局前チェックで既存の自動効果を飛ばす | 必須自動効果は先に完了、任意対象効果だけをスキップする |
| 生成グリッチが再接続ごとに再生される | 安定event IDとクライアントの既読管理を使う |
| 色分けで意味が伝わらない | visual roleラベル・効果文・ARIA名を併用する |
| 一覧の高さが大きすぎて操作を妨げる | 表示設定はローカル限定、モバイルの動的上限とリセット値を持つ |
| 長い名称の省略で意味が失われる | 顔用短縮名だけを省略し、正式名はARIA・title・詳細パネルに残す |

## 13. 実装開始前の確定事項

以下は追加確認を待たずに実装してよい合理的な決定である。

1. 効果対象は30秒、カードを選択してから確定する二段階操作にする。
2. 終局後の任意対象選択は実行しない。
3. ルール用語は、その凍結デッキに存在する概念だけを表示する。
4. 能力のコピーとカードの複製は、利用者向けには必ず別語で表示する。
5. ノイズの正体は、演出・履歴・対象選択でも所有者以外へ出さない。
6. The Emperorは金、他Tarotは意味別の控えめな色、Joker/3/2は青基調の薄い青緑にする。
7. デッキ一覧の高さは個人のローカル表示設定とし、対局設定や保存プリセットへ入れない。
