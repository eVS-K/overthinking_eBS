{
  "name": "overthinking-ebs",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "npm run test:node",
    "test:node": "npm run generate-ranked-values && node --test",
    "check": "node scripts/check-syntax.js",
    "test:coverage": "npm run generate-ranked-values && node scripts/run-core-coverage.js",
    "test:postgres": "npm run generate-ranked-values && node --test --test-concurrency=1 postgres-integration.js",
    "test:ci": "npm run check && npm test && npm run test:coverage",
    "generate-ranked-values": "node scripts/generate-ranked-values.js",
    "migrate": "node scripts/migrate.js",
    "rotate-ranked-season": "node scripts/rotate-ranked-season.js --confirm",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.13.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
